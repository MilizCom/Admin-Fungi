name: fungi_casheer
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  flutter_lints: ^5.0.0
  intl: ^0.19.0
  get: ^4.6.6
  image_picker: ^1.0.7  
  path_provider: ^2.1.2
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
