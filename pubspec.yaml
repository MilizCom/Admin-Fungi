name: admin_fungi
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  get: ^4.6.5
  cloud_firestore: ^4.9.1
  firebase_core: ^2.15.1
  intl: ^0.18.1
  fl_chart: ^0.66.0
  excel: ^4.0.0 # Membuat file Excel
  path_provider: ^2.1.1 # Akses penyimpanan
  share_plus: ^7.2.0 # Share file
  open_file: ^3.3.2 # (Opsional) Langsung buka file
  http: ^1.1.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
